# camoufox-nix
https://github.com/daijro/camoufox for nixos via docker

# Quick setup:
```shell
./run-camoufox.sh
```

#./run-camoufox.sh does build camoufox-app docker image then export Plasma wayland credentials via mounts
